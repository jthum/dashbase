# Login Screen / Split

A page-level authentication shell that pairs contextual onboarding copy with the canonical login-box panel on larger viewports.

## Imports

```svelte
<script lang="ts">
  import { AuthLoginScreenSplit } from "@dashbase/svelte/patterns/auth/login-screen/split";
</script>
```

```ts
import { AuthLoginScreenSplit, authLoginScreenSplitAssets } from "@dashbase/svelte/patterns/auth/login-screen/split/manual";
```

## Scope

- Category: `auth`
- Family: `login-screen`
- Variant: `split`
- Scope: `page`

## Usage

```svelte
<script lang="ts">
  import { AuthLoginScreenSplit } from "@dashbase/svelte/patterns/auth/login-screen/split";
</script>

<AuthLoginScreenSplit />
```
