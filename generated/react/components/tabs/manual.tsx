/* eslint-disable react-refresh/only-export-components */
/* Generated by scripts/generate-react.ts for Tabs. */
import React, { forwardRef } from "react";
import { cx, type DashbaseAssetManifest, type DashbaseCustomElementProps } from "../../runtime.ts";

export const tabsAssets = {
  css: [
  "dist/components/tabs/tabs.css"
],
  js: [
  "dist/components/tabs/tabs.js"
],
} as const satisfies DashbaseAssetManifest;

export type TabsProps = DashbaseCustomElementProps & {
  vertical?: boolean;
};

export const Tabs = forwardRef<HTMLElement, TabsProps>(function Tabs({ className, vertical, ...props }: TabsProps, ref) {
  return (
    <ui-tabs
      ref={ref}
      {...props}
      className={cx(
        className,
        vertical && "vertical",
      )}
    >
      {props.children}
    </ui-tabs>
  );
});
Tabs.displayName = "Tabs";

export type TabsListProps = DashbaseCustomElementProps & {
  orientation?: "horizontal" | "vertical";
};

export const TabsList = forwardRef<HTMLElement, TabsListProps>(function TabsList({ className, orientation, ...props }: TabsListProps, ref) {
  return (
    <tab-list
      ref={ref}
      role="tablist"
      aria-orientation={orientation}
      {...props}
      className={className}
    >
      {props.children}
    </tab-list>
  );
});
TabsList.displayName = "TabsList";

export type TabsTriggerProps = React.ComponentPropsWithoutRef<"button"> & {
};

export const TabsTrigger = forwardRef<HTMLButtonElement, TabsTriggerProps>(function TabsTrigger({ className, ...props }: TabsTriggerProps, ref) {
  return (
    <button
      ref={ref}
      role="tab"
      {...props}
      className={className}
    >
      {props.children}
    </button>
  );
});
TabsTrigger.displayName = "TabsTrigger";

export type TabsContentProps = DashbaseCustomElementProps & {
};

export const TabsContent = forwardRef<HTMLElement, TabsContentProps>(function TabsContent({ className, ...props }: TabsContentProps, ref) {
  return (
    <tab-panel
      ref={ref}
      role="tabpanel"
      {...props}
      className={className}
    >
      {props.children}
    </tab-panel>
  );
});
TabsContent.displayName = "TabsContent";
