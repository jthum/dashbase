/* eslint-disable react-refresh/only-export-components */
/* Generated by scripts/targets/react/generate.ts for Popover Panel. */
import React, { forwardRef } from "react";
import { cx, type DashbaseAssetManifest, type DashbaseCustomElementProps } from "../../runtime.ts";

export const popoverAssets = {
  css: [
  "dist/components/panel/panel.css",
  "dist/components/popover/popover.css"
],
  js: [
  "dist/components/popover/popover.js"
],
} as const satisfies DashbaseAssetManifest;

export type PopoverPanelProps = DashbaseCustomElementProps & {
  wide?: boolean;
  alignEnd?: boolean;
  above?: boolean;
  submenu?: boolean;
  contextMenu?: boolean;
};

export const PopoverPanel = forwardRef<HTMLElement, PopoverPanelProps>(function PopoverPanel({ className, wide, alignEnd, above, submenu, contextMenu, ...props }: PopoverPanelProps, ref) {
  return (
    <popover-panel
      ref={ref}
      popover
      {...props}
      className={cx(
        className,
        wide && "wide",
        alignEnd && "align-end",
        above && "above",
        submenu && "submenu",
        contextMenu && "context-menu",
      )}
    >
      {props.children}
    </popover-panel>
  );
});
PopoverPanel.displayName = "PopoverPanel";

export type PopoverHeaderProps = DashbaseCustomElementProps & {
};

export const PopoverHeader = forwardRef<HTMLElement, PopoverHeaderProps>(function PopoverHeader({ className, ...props }: PopoverHeaderProps, ref) {
  return (
    <panel-header
      ref={ref}
      {...props}
      className={className}
    >
      {props.children}
    </panel-header>
  );
});
PopoverHeader.displayName = "PopoverHeader";

export type PopoverContentProps = DashbaseCustomElementProps & {
};

export const PopoverContent = forwardRef<HTMLElement, PopoverContentProps>(function PopoverContent({ className, ...props }: PopoverContentProps, ref) {
  return (
    <panel-content
      ref={ref}
      {...props}
      className={className}
    >
      {props.children}
    </panel-content>
  );
});
PopoverContent.displayName = "PopoverContent";

export type PopoverFooterProps = DashbaseCustomElementProps & {
};

export const PopoverFooter = forwardRef<HTMLElement, PopoverFooterProps>(function PopoverFooter({ className, ...props }: PopoverFooterProps, ref) {
  return (
    <panel-footer
      ref={ref}
      {...props}
      className={className}
    >
      {props.children}
    </panel-footer>
  );
});
PopoverFooter.displayName = "PopoverFooter";
